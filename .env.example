# Copy to .env and fill in. Only required for LLM-assisted steps
# (threat-intel synthesis, novel hypothesis generation, report/disclosure
# drafting). Scope lock, the approval gate, tool execution, and audit
# logging all work without it.
ANTHROPIC_API_KEY=
